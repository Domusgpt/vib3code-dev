{
  "name": "vib34d-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "dev": "pnpm -r --parallel --filter vib34d-demo --filter vib34d-editor run dev",
    "preview": "pnpm -r --parallel --filter vib34d-demo --filter vib34d-editor run preview"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "jsdom": "^27.0.1",
    "typescript": "^5.4.0",
    "vite": "^5.1.0",
    "vitest": "^1.4.0"
  }
}
